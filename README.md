# Cloud Inventory awareness assistant

 

# Usage

## Cognises config

## SSH Config

It is recommended to set the generated output to a secondary config file and use `Include ~/.ssh/gererated_config`